{
  "name": "gemini-schedule-demo",
  "version": "1.0.0",
  "description": "A demo application showing how to use Google's Gemini API to create student schedules",
  "main": "dist/tagsearch-tests.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/tagsearch-tests.js",
    "dev": "ts-node --transpile-only tagsearch-tests.ts",
    "test": "npm run build && node dist/tagsearch-tests.js",
    "manual": "npm run build && node -e \"require('./dist/tagsearch-tests.js').testManualScheduling()\"",
    "llm": "npm run build && node -e \"require('./dist/tagsearch-tests.js').testLLMScheduling()\"",
    "mixed": "npm run build && node -e \"require('./dist/tagsearch-tests.js').testMixedScheduling()\""
  },
  "keywords": ["gemini", "ai", "schedule", "student", "demo", "typescript"],
  "author": "Student Demo",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
